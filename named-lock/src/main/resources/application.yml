spring:
  datasource:
    jdbcUrl: jdbc:mysql:///test_db?serverTimezone=UTC&characterEncoding=UTF-8&useSSL=false
    username:
    password:
    driverClassName: com.mysql.cj.jdbc.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jdbc:
  datasource:
    jdbcUrl: jdbc:mysql:///test_db?serverTimezone=UTC&characterEncoding=UTF-8&useSSL=false
    username:
    password:
    driverClassName: com.mysql.cj.jdbc.Driver
